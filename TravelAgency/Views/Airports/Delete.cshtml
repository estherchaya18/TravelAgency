@model TravelAgency.Models.Airports

@{
    ViewData["Title"] = "Delete";
}

    <div style="margin-right: auto;
     width:fit-content;
     font-size:large;
     font-weight:100;
     text-emphasis-color:brown;
     margin-left: auto;
     background-color:sandybrown;
     color:black;
     padding: 10vh;
     border-radius: 0 0 10px 10px;">
        <h2>Delete</h2>

        <h3>Are you sure you want to delete this?</h3>

        <h4>Airports</h4>

        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.AirportDetailes)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.AirportDetailes)
            </dd>
        </dl>

        <form asp-action="Delete" style="background-color:sandybrown;color:black;">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Delete" class="btn btn-default" /> |
            <a asp-action="Index">Back to List</a>
        </form>
    </div>
